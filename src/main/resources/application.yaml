spring:
  application:
    name: producttest

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: ${apps.kafka.groupId}


app:
  kafka:
    producer:
      ack-mode: true
      type-mapping: ""
      topic-producer: product-messaging
      dead-box: dead-box
    listener:
      group-id: ""
      auto-offset: earliest
      order-topic: order-created

